<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:binding="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="context"
            type="xyz.ttyz.tourfrxohc.dialog.SingleSelectDialog"/>
        <variable
            name="adapter"
            type="xyz.ttyz.toubasemvvm.adapter.utils.BaseEmptyAdapterParent"/>
        <variable
            name="itemDecoration"
            type="androidx.recyclerview.widget.RecyclerView.ItemDecoration" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="20dp"
        android:paddingTop="20dp"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">
        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/viewholder_select_str"
            binding:adapter="@{adapter}"
            binding:itemDecoration="@{itemDecoration}"
            tools:itemCount="1"
            android:background="@drawable/bg_white_conor" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:textColor="@color/black"
            android:text="取消"
            android:textStyle="bold"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginTop="10dp"
            binding:onClickCommand="@{context.clickCancelCommand}"
            android:background="@drawable/pressed_bg_white"/>


    </LinearLayout>
</layout>